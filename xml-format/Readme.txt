How do you think XML relates to HTML? 
They are both tagged structure files.

What are some benefits of something like HTML using XML's syntax?  
It is easier to select entities. XML also work with a broad number of programs. It is a universal file format.

What are some disadvantages? 
XML seems very sturdy compared to HTML. XML is less intuitive where as HTML is more flexible.